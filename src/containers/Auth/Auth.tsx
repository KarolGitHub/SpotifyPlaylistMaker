import React, { FunctionComponent } from "react";

import classes from "./Auth.module.scss";

type Props = {};
const Auth: FunctionComponent<Props> = () => {
  return <div className={classes.Auth}></div>;
};

export default Auth;
